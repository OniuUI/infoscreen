version: '3.8'

services:
    frontend:
        build: ./infoscreen-front
        ports:
            - '3000:3000'
        depends_on:
            - backend

    backend:
        build: ./infoscreen-backend
        ports:
            - '3001:3001'
